<Window x:Class="OrderAllocationApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:OrderAllocationApp.ViewModels"
        Title="注文引当アプリ" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <DockPanel Margin="8">

        <!-- 上部ボタンエリア -->
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="0,0,0,8">
            <Button Content="引当実行"
                    Command="{Binding ExecuteAllocationCommand}"
                    Width="100"
                    Height="30" />
        </StackPanel>

        <!-- メインコンテンツ: 注文 / 在庫 / 引当結果 を縦に並べる -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="6" />
                <RowDefinition Height="*" />
                <RowDefinition Height="6" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 注文一覧 -->
            <GroupBox Grid.Row="0" Header="注文一覧">
                <DataGrid ItemsSource="{Binding Orders}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="True"
                          CanUserDeleteRows="True"
                          ColumnWidth="*">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="注文ID"    Binding="{Binding OrderId}" />
                        <DataGridTextColumn Header="商品コード" Binding="{Binding ProductCode}" />
                        <DataGridTextColumn Header="数量"      Binding="{Binding Quantity}" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- 在庫一覧 -->
            <GroupBox Grid.Row="2" Header="在庫一覧">
                <DataGrid ItemsSource="{Binding Stocks}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="True"
                          CanUserDeleteRows="True"
                          ColumnWidth="*">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="商品コード" Binding="{Binding ProductCode}" />
                        <DataGridTextColumn Header="数量"      Binding="{Binding Quantity}" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- 引当結果 -->
            <GroupBox Grid.Row="4" Header="引当結果">
                <DataGrid ItemsSource="{Binding AllocationResults}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          ColumnWidth="*">

                    <!-- 行背景色: 成功=薄緑 / 失敗=薄赤（DataTrigger で制御、IValueConverter 不使用）-->
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                    <Setter Property="Background" Value="#E8F5E9" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                    <Setter Property="Background" Value="#FFEBEE" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="注文ID"   Binding="{Binding OrderId}" />
                        <DataGridTextColumn Header="成否"     Binding="{Binding IsSuccess}" />
                        <DataGridTextColumn Header="引当数量" Binding="{Binding AllocatedQuantity}" />
                        <DataGridTextColumn Header="不足数量" Binding="{Binding ShortageQuantity}" />

                        <!-- メッセージ列: 成功=青文字 / 失敗=赤文字（DataTrigger で制御）-->
                        <DataGridTemplateColumn Header="メッセージ" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Message}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                                        <Setter Property="Foreground" Value="#1565C0" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                                        <Setter Property="Foreground" Value="#B71C1C" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </DockPanel>
</Window>
